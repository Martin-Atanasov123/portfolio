# Copy this file to `.env` and fill in your key.
# Web3Forms — contact form delivery (no backend needed).
# 1. Go to https://web3forms.com
# 2. Enter your email (matanasov573@gmail.com) to get a free Access Key.
# 3. Paste the key below (no quotes), then restart `npm run dev`.
# This key is safe to expose client-side (Web3Forms treats it as public).
VITE_WEB3FORMS_ACCESS_KEY=
